import app from '../app.json';

/** Marketing / settings label — keep in sync with `expo.version` in app.json. */
export const APP_VERSION = app.expo.version;

export const APP_VERSION_SETTINGS_LABEL = `${APP_VERSION} (Beta)`;
